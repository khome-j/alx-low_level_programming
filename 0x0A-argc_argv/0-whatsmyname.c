#include <stdio.h>
/**
 * main - This program prints its name followed by a new line.
 * @argc: argument counts
 * @argv: argument vector; which is basically a one dimensional array.
 * Return: Always 0.
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
    int count = 0;

    printf("my name is %s\n", argv[count]);
    return (0);
}
