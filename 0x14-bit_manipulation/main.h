#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int _pow(int a, int b);
int _strlen(const char* b);
int _atoi(char b);

unsigned int binary_to_unint(const char *b);

#endif /*MAIN_H*/
